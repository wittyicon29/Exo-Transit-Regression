% For Visualization purposes only

boxchart(accuracy);

contour(normalizedFeatures);

mesh(normalizedFeatures);

surf(normalizedFeatures);

contourf(normalizedFeatures);

ribbon(normalizedFeatures);

contour3(normalizedFeatures);

waterfall(normalizedFeatures);

meshz(normalizedFeatures);

meshc(normalizedFeatures);

parallelplot(normalizedFeatures);

qqplot(normalizedFeatures);

 stackedplot(data);
 
contour(normalizedFeatures);

waterfall(normalizedFeatures);

mesh(normalizedFeatures);

surf(normalizedFeatures);

mesh(normalizedFeatures);

histogram(X_train);

contour(X_train);

surf(X_train);

scatter(confusionMat(:,1),confusionMat(:,2));

histogram(confusionMat);

histogram(confusionMat_mlp);

bar3(confusionMat_svm);

swarmchart(confusionMat_svm(:,1),confusionMat_svm(:,2));

plotmatrix(confusionMat_svm);

surf(normalizedFeatures);

mesh(normalizedFeatures);

